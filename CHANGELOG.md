# 变更记录

## 0.19.8 - 2026-08-29

- 坐标、网格和旋转题把提交按钮提升到次要编辑操作之前，固定高度容器内不再先显示“撤销/清空”却把提交裁到屏幕外。
- Android 增加加密、限长的本机诊断记录，覆盖未捕获崩溃、后台连接、工作流、配对和 Agent 任务失败。
- 诊断只保存异常类型、CaptchaMesh 自身栈帧及版本信息，不保存异常消息、Key、Token、Cookie、网址或任务内容。
- “记录”页增加 48dp 的“复制诊断”入口和成功反馈；清空记录时会同时删除活动与诊断记录。
- 电脑本地桥增加权限 `0600`、上限 256 KiB 的脱敏 JSONL 诊断，并提供 `captchamesh logs` 与 `captchamesh logs --clear`。

## 0.19.7 - 2026-08-29

- 修复第三方 App 直接打开荣耀“应用启动管理”时因厂商签名权限限制导致的崩溃。
- 荣耀按钮改为打开对第三方可访问的系统管家主页，并给出“应用启动管理 → CaptchaMesh → 关闭自动管理并允许三项后台活动”的明确操作。
- 所有厂商设置入口均捕获权限或页面缺失异常，并回退到 Android 系统设置，不再中断后台服务与 App 进程。

## 0.19.6 - 2026-08-29

- 荣耀真机证明 Android 标准电池优化豁免后 MagicOS 仍会冻结进程；设置页因此增加荣耀“应用启动管理”直达入口。
- 荣耀设备会明确提示关闭自动管理并允许自启动、关联启动和后台活动；在真实后台到题测试通过前，自检不会把后台状态标为正常。
- 常驻通知同步采用保守状态，避免把 Android 标准豁免误报成荣耀后台接题已经可靠。

## 0.19.5 - 2026-08-29

- 修复荣耀等系统退到后台后冻结 Agent 长连接的发布阻断：设置页改为通过 Android 系统确认弹窗申请后台豁免，应用不会静默更改系统设置。
- 未获后台豁免时，设置页、自检与前台服务通知均明确标记后台提醒可能延迟，不再把 WakeLock 误报为已保活。
- 获得用户确认后自动刷新状态并重启后台连接，继续使用有超时且可释放的局部 WakeLock。

## 0.19.4 - 2026-08-29

- 修复 Agent 结果回传后“返回待命”按钮可见但不执行状态复位的问题。
- Android Agent 后台接题服务使用有超时、定期续期且随服务停止释放的局部 WakeLock，降低荣耀等系统切到后台后冻结轮询的概率。
- 设置页增加后台保护状态与系统入口；应用只提供说明和入口，不静默修改电池优化设置。

## 0.19.3 - 2026-08-29

- Android Agent 任务页增加并发任务列表，明确显示正在处理与等待中的任务数量和状态。
- 手机后台将最多 32 个端到端加密任务持久入队，只有本机落盘成功后才确认 Hub，单个任务不再阻塞后续任务接收。
- 电脑端本地桥允许多个 Agent 请求并发提交，并按 `taskId` 分流乱序返回结果，避免误领或丢弃其他任务结果。
- 手机上的人工 CAPTCHA 仍保持单一可见焦点，工作流与 Agent 任务通过公平队列依次进入人工交互，不实现自动绕过。

## 0.19.2 - 2026-08-28

- README 安装流程现在会一并安装 `captchamesh-adapter` Agent Skill，并提供 `skill install/status` 命令。
- wheel、sdist、CI 与发布流水线强制包含并验证完整 Skill；升级时保护用户修改和非托管目录。
- Skill 检查器改为可移植的 `captchamesh skill inspect`，不再依赖用户项目存在仓库内 `.skill` 路径。
- Release 必须等待完整 CI，通过后附带 SPDX SBOM、SHA-256 校验和与 GitHub 构建来源证明。
- GitHub Actions 升级到 Node.js 24 运行时版本并固定提交 SHA，安全任务增加完整 Git 历史密钥扫描。
- 增加社区行为准则、支持入口、Issue/PR 模板和 Dependabot 更新策略。
- Android 设置页增加应用内“扫码配对”按钮，不再依赖系统相机识别深链。
- 扫描页仅接受 QR 码，并复用既有 `captchamesh://pair` 一次性令牌与端到端配对校验。
- 增加取消、无效二维码和重新配对反馈，以及相机能力与 Android UI 契约测试。
- 重新配对时清除旧的待处理加密信封，避免历史任务阻塞后台轮询和新任务通知。
- Android 冷启动会先解密加载已保存的 Broker/API Key，再读取电脑工作流；普通刷新不再重写连接密钥。

## 0.19.1 - 2026-08-28

- Android 首次启动时将空 API Key 作为“待配置”状态，不再发起无效鉴权请求或误报 Key 无效。
- 连接失败后保留用户当前页面；HTTP 401/403 仅在 API Key 输入框显示修复提示。
- 自检明确区分 API Key “未配置”与“无效”，并补充对应 Android UI 契约测试。

## 0.19.0 - 2026-08-26

- 增加独立 Hub 发布包与 Ubuntu/Debian 交互式安装器，支持原地升级和公网健康检查。
- Cloudflare Tunnel 改为无回显 token 输入、专用账户和权限受限 token 文件。
- 增加可选的 GPG 加密每日备份及 systemd 定时器。
- GitHub Release 改为稳定密钥签名的 Android release APK，并强制校验版本一致性。
- `0.18.x` debug APK 需要卸载后安装一次 `0.19.0` 并重新配对，之后可正常覆盖升级。
- CI 增加 Hub 包边界、完整成员和安装器语法检查。

## 0.18.3 - 2026-08-25

- 明确区分默认 Agent API 模式与可选的手机启动电脑工作流模式。
- Android 默认进入 Agent 任务页，并将“注册机”统一改为“工作流”。
- 设置页增加 CAPTCHA 到达提醒开关和 Android 系统通知设置入口。
- 重写接入文档与 CaptchaMesh Adapter Skill，避免混用本地桥和工作流 Hub 接口。

## 0.18.2 - 2026-08-25

- 重做 Android 启动器图标，缩小盾牌主体并为圆形、圆角方形裁切保留安全边距。
- 增加独立深色启动器背景、主题图标前景和圆形图标声明。

## 0.18.1 - 2026-08-25

- 配对能力令牌改用 URL fragment，并从浏览器地址栏和请求路径中清除。
- 默认隐藏非交互配对令牌和本机 API Key，增加受限文件交接方式。
- 强化本机密钥文件的普通文件、链接数和权限校验。
- 增加发布包边界检查、完整测试脚本和开源测试指南。
- 重构 README，补充安装、能力、架构、安全和测试入口。

## 0.18.0 - 2026-08-25

- 增加电脑端本地加密桥、一次性二维码配对和 Android 后台通知。
- 增加端到端加密中继协议及官方/自建 Hub 模式。
- 增加图片文字、坐标、网格、旋转和多种网页挑战的手机人工界面。
- 增加常用 2Captcha API v1/v2 兼容接口和 Python 适配器。
- 限制 Host、代理头、端口、请求体、并发和速率，并强化 systemd 沙箱。
- 清理个人配置和构建产物，补充可复现构建、CI 与开源项目文档。
